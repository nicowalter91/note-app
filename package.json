{
    "name": "note-app-root",
    "version": "1.0.0",
    "scripts": {
        "install:all": "npm install --prefix backend && npm install --prefix frontend/notes-app",
        "start:backend": "npm  start --prefix backend",
        "start:frontend": "npm run dev --prefix frontend/notes-app"
    },
    "dependencies": {
        "nodemailer": "^7.0.3",
        "react-datepicker": "^8.4.0",
        "react-modal": "^3.16.3"
    }
}
